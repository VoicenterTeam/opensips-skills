{
  "fixture": "version-mismatch.cfg",
  "expected_opensips_version": "3.6.x",
  "expected_profile": "L1",
  "expected_findings": [
    {
      "id_anchor": "F1",
      "rule_id": "OSIPS-SEC-CFG-VERSION-MISMATCH",
      "severity": "medium",
      "confidence": "high",
      "verification_status": "deterministic_confirmed",
      "kind": "vulnerability",
      "approximate_location": {
        "anchor": "loadmodule \"tls_wolfssl.so\"",
        "tolerance_lines": 2
      },
      "validation_notes": "Synthetic rule_id placeholder. The actual rule will be authored in the config_hygiene family. Engine must detect that the cfg loads tls_wolfssl (introduced in 3.6) while the operator-supplied or declared version metadata indicates 3.4.x. The mismatch is itself a security concern: rules version-gated to 3.4 may not apply correctly, and CVE detection may be miscalibrated."
    }
  ],
  "expected_informationals": [],
  "must_not_fire": [],
  "validation_notes": [
    "expected_opensips_version is set to '3.6.x' because the cfg's strongest evidence (tls_wolfssl present) wins over the operator's stated 3.4 declaration. Per 14_DETECTION_ENGINE.md, the engine corroborates declared version against signature evidence; signature wins on conflict.",
    "AnalysisContext should record opensips_version_declared='3.4.10' (from the comment) and opensips_version_detected='3.6.x' (from tls_wolfssl signature), and opensips_version_confidence='high' (the contradiction is unambiguous — tls_wolfssl simply doesn't exist in 3.4).",
    "If the engine silently trusts the declared version, version-gated rules may run incorrectly: e.g., CVE-2026-25554 detection (rule applies_if_opensips_version >=3.1, <3.6.4) might be skipped if engine thinks this is 3.4, when in fact this cfg IS in the vulnerable range.",
    "Synthetic rule_id OSIPS-SEC-CFG-VERSION-MISMATCH is a placeholder; update once the actual rule is authored in config_hygiene family."
  ]
}
