{
  "fixture": "false-positive-cvss-gate.cfg",
  "expected_opensips_version": "3.6.5",
  "expected_profile": "L1",
  "expected_findings": [],
  "expected_informationals": [],
  "must_not_fire": [
    "OSIPS-SEC-AUTH-004",
    "OSIPS-SEC-MI-001",
    "OSIPS-SEC-INJ-001",
    "OSIPS-SEC-TLS-001",
    "OSIPS-SEC-RELAY-001"
  ],
  "validation_notes": [
    "AUTH-004 (CVE-2026-25554 detector, projected) has frontmatter applies_if_opensips_version: '>=3.1, <3.6.4'. The deployed version 3.6.5 is OUTSIDE this range. The rule MUST be skipped, not fired.",
    "If AUTH-004 fires here, the engine's version-gating logic in 14_DETECTION_ENGINE.md § applies_if_opensips_version is failing — either the version detection is producing wrong output, or the gate comparison logic is buggy.",
    "The skip should be visible in Appendix D's rules_skipped list with reason='version range excluded' or similar.",
    "This fixture's contract is the inverse of vulnerable-mixed.cfg's F1: same structural pattern (auth_jwt + db_mode=1), but vulnerable-mixed declares 3.6.2 (vulnerable) while this fixture declares 3.6.5 (patched). The engine must distinguish.",
    "If this fixture and vulnerable-mixed both fire AUTH-004 the same way, version-gating is non-functional. If neither fires, the rule is broken in the other direction."
  ]
}
