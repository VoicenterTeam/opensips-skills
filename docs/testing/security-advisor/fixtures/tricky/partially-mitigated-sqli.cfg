####### partially-mitigated-sqli.cfg
# Tricky fixture: SQL injection partially mitigated. Two avp_db_query
# call sites; one is properly sanitized via s.escape.common, the other
# is not. Tests that the engine fires only on the unsanitized site,
# not on the sanitized one — and produces TWO distinct findings, not
# one collapsed finding, when the same rule fires on different sinks.
#
# Target: OpenSIPS 3.5.4. Profile: L1.

log_level=2
children=4
listen=udp:0.0.0.0:5060
mpath="/usr/lib/x86_64-linux-gnu/opensips/modules/"

loadmodule "signaling.so"
loadmodule "sl.so"
loadmodule "tm.so"
loadmodule "rr.so"
loadmodule "maxfwd.so"
loadmodule "sipmsgops.so"
loadmodule "auth.so"
loadmodule "auth_db.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "avpops.so"
loadmodule "tls_mgm.so"
loadmodule "proto_udp.so"
loadmodule "proto_tls.so"
loadmodule "pike.so"
loadmodule "db_mysql.so"

modparam("auth_db", "db_url", "mysql://opensips@db.internal/opensips")
modparam("auth_db", "calculate_ha1", 0)
modparam("auth_db", "password_column", "ha1")
modparam("usrloc", "db_url", "mysql://opensips@db.internal/opensips")
modparam("tls_mgm", "verify_cert", "[default]1")
modparam("tls_mgm", "require_cert", "[default]1")
modparam("tls_mgm", "ca_list", "[default]/etc/opensips/tls/ca.pem")
modparam("tls_mgm", "certificate", "[default]/etc/opensips/tls/cert.pem")
modparam("tls_mgm", "private_key", "[default]/etc/opensips/tls/key.pem")
modparam("pike", "sampling_time_unit", 2)
modparam("pike", "reqs_density_per_unit", 20)

alias=proxy.example.com

####### Routing — two SQL call sites, only one mitigated
request_route {
    if (!mf_process_maxfwd_header("10")) {
        send_reply(483, "Too Many Hops");
        exit;
    }
    if (!pike_check_req()) { exit; }

    if (!is_myself("$rd") && !has_totag()) {
        send_reply(403, "Forbidden");
        exit;
    }

    if (has_totag()) {
        if (loose_route()) { t_relay(); } else { send_reply(404, "Not here"); }
        exit;
    }

    if (is_method("REGISTER")) {
        if (!www_authenticate("$td", "subscriber")) {
            www_challenge("$td", 0);
            exit;
        }

        # MITIGATED: $fU is escaped before SQL interpolation.
        $avp(safe_fU) = $(fU{s.escape.common});
        avp_db_query("SELECT id FROM users WHERE username='$avp(safe_fU)'",
                     "$avp(uid)");

        save("location");
        exit;
    }

    if (is_method("INVITE")) {
        if (!proxy_authorize("$td", "subscriber")) {
            proxy_challenge("$td", "0");
            exit;
        }
        consume_credentials();

        # NOT MITIGATED: $rU is interpolated raw. SQL injection.
        avp_db_query("SELECT route FROM dialplan WHERE prefix='$rU'",
                     "$avp(route)");

        record_route();
        t_relay();
    }
}

failure_route {
    if (t_was_cancelled()) { exit; }
}
