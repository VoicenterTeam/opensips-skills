####### permissions-allow-routing.cfg
# Tricky fixture: t_relay() reached only after allow_routing() guard.
# Exercises FP class permissions_allow_routing_reachable for RELAY-001.
# Target: OpenSIPS 3.5.4. Profile: L1.
#
# RELAY-001 structural matcher fires on the t_relay() site (no preceding
# is_myself / has_totag / loose_route in the SAME route block). Semantic
# judge must read the cfg, identify the allow_routing() gate in the
# preceding if-block, and return applies: yes for the FP class. Finding
# becomes judge_dissented and is suppressed-with-audit.

log_level=2
children=4
listen=udp:10.0.1.10:5060
listen=tls:10.0.1.10:5061
mpath="/usr/lib/x86_64-linux-gnu/opensips/modules/"

loadmodule "signaling.so"
loadmodule "sl.so"
loadmodule "tm.so"
loadmodule "rr.so"
loadmodule "maxfwd.so"
loadmodule "sipmsgops.so"
loadmodule "auth.so"
loadmodule "auth_db.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "tls_mgm.so"
loadmodule "proto_udp.so"
loadmodule "proto_tls.so"
loadmodule "permissions.so"
modparam("permissions", "address_table", "address")
modparam("permissions", "trusted_table", "trusted")
modparam("permissions", "db_url", "mysql://opensips@db.internal/opensips")
loadmodule "pike.so"
modparam("pike", "sampling_time_unit", 2)
modparam("pike", "reqs_density_per_unit", 20)
loadmodule "acc.so"
loadmodule "db_mysql.so"

modparam("auth_db", "db_url", "mysql://opensips@db.internal/opensips")
modparam("auth_db", "calculate_ha1", 0)
modparam("auth_db", "password_column", "ha1")
modparam("usrloc", "db_url", "mysql://opensips@db.internal/opensips")
modparam("usrloc", "db_mode", 2)

modparam("tls_mgm", "verify_cert", "[default]1")
modparam("tls_mgm", "require_cert", "[default]1")
modparam("tls_mgm", "ca_list", "[default]/etc/opensips/tls/ca.pem")
modparam("tls_mgm", "certificate", "[default]/etc/opensips/tls/cert.pem")
modparam("tls_mgm", "private_key", "[default]/etc/opensips/tls/key.pem")

alias=proxy.example.com

####### Routing — t_relay gated by allow_routing()
request_route {
    if (!mf_process_maxfwd_header("10")) {
        send_reply(483, "Too Many Hops");
        exit;
    }
    if (!pike_check_req()) {
        xlog("L_ALERT", "pike block from $si\n");
        exit;
    }

    if (has_totag()) {
        if (loose_route()) { t_relay(); } else { send_reply(404, "Not here"); }
        exit;
    }

    if (is_method("REGISTER")) {
        route(handle_register);
        exit;
    }

    # Authorization gate via permissions module.
    # allow_routing() consults the trusted table for source-IP authorization.
    if (!allow_routing()) {
        xlog("L_NOTICE", "blocked unauthorized source $si\n");
        send_reply(403, "Forbidden");
        exit;
    }

    # Auth check (still needed for credential verification).
    if (is_method("INVITE|MESSAGE|SUBSCRIBE")) {
        if (!proxy_authorize("$td", "subscriber")) {
            proxy_challenge("$td", "0");
            exit;
        }
        consume_credentials();
    }

    record_route();
    t_relay();
}

route[handle_register] {
    if (!www_authenticate("$td", "subscriber")) {
        www_challenge("$td", 0);
        exit;
    }
    save("location");
}

failure_route {
    if (t_was_cancelled()) { exit; }
}

onreply_route {
    if (status =~ "1[0-9][0-9]") { exit; }
}
