# sipmsgops Module Reference
<!-- generated-from: data/3.6/modules/sipmsgops.json
     generator-version: 0.1.0
     opensips-version: 3.6
     doc-type: module -->

Reference for the OpenSIPs 3.6 sipmsgops module. Read this file when configuring or debugging the sipmsgops module: signature, parameters, return codes, exported MI commands, statistics, events, and configuration examples.

## Contents

- [Overview](#overview)
- [Dependencies](#dependencies)
- [Exported Functions](#exported-functions)
- [Configuration Examples](#configuration-examples)

## Overview

The module implements SIP based operations over the messages processed by OpenSIPS. SIP is a text based protocol and the module provides a large set of very useful functions to manipulate the message at SIP level, e.g., inserting new headers or deleting them, check for method type, etc.

## Dependencies

### OpenSIPs Modules

None.

### External Libraries

None.

## Exported Functions

### `add_body_part(body, mime[, headers])`

This function can be used to add a new body part to the message body. If another part already exist, body of the message will be converted to a multi-part body automatically.

**Parameters:**

- `body` *(string, required)* — the content of the body part to be added
- `headers` *(string, optional)* — optional list of SIP headers (fully defined, including the header separator) to be pushed into this part next to the _Content-Type_ header.
- `mime` *(string, required)* — the mime string for the body part to be added

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Add a text/plain body part.

```opensips
...
add_body_part("Hello World!", "text/plain");
...
```

### `append_body_to_reply(txt)`

Append 'txt' as body to all replies that will be generated by OpenSIPS for this request.

Multiple calls will override the already set body.

NOTE: the function does not add any Content-Type hdr to match the body, so you should use "append_to_reply()" to do that.

**Parameters:**

- `txt` *(string, required)* — body of the SIP reply

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, ERROR_ROUTE

**Related:**

- `append_to_reply()`

**Example.** append_to_reply usage.

```opensips
append_body_to_reply( $var(sdp_body) );
```

### `append_hf(txt[, hdr_anchor])`

Appends 'txt' as header after the last header field. If 'hdr_anchor' is given, 'txt' will be appended after the first occurrence of 'hdr_anchor' instead.

Note: Headers which are added in main route cannot be removed in further routes (e.g. failure routes). So, the idea is not to add there any headers that you might want to remove later. To add headers temporarily, use the branch route because the changes you do there are per-branch.

**Parameters:**

- `hdr_anchor` *(string, optional)* — Header name after which the 'txt' is appended.
- `txt` *(string, required)* — Header field to be appended.

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** append_hf usage.

```opensips
append_hf("P-hint: VOICEMAIL\\r\\n");
append_hf("From-username: $fU\\r\\n");
append_hf("From-username: $fU\\r\\n", "Call-ID");
```

### `append_time()`

Adds a time header to the reply of the request. You must use it before functions that are likely to send a reply, e.g., save() from 'registrar' module. Header format is: “Date: %a, %d %b %Y %H:%M:%S GMT”, with the legend:

*   _%a_ abbreviated week of day name (locale)
    
*   _%d_ day of month as decimal number
    
*   _%b_ abbreviated month name (locale)
    
*   _%Y_ year with century
    
*   _%H_ hour
    
*   _%M_ minutes
    
*   _%S_ seconds

**Return codes:**

- `true` — if a header was successfully appended

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** append_time usage.

```opensips
append_time();
```

### `append_to_reply(txt)`

Append 'txt' as header to all replies that will be generated by OpenSIPS for this request.

**Parameters:**

- `txt` *(string, required)* — SIP header field, value and CRLF marker.

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, ERROR_ROUTE

**Example.** append_to_reply usage.

```opensips
append_to_reply("Foo: bar\\r\\n");
append_to_reply("Foo: $rm at $Ts\\r\\n");
```

### `append_urihf(prefix, suffix)`

Append header field name with original Request-URI in middle.

**Parameters:**

- `prefix` *(string, required)* — string (usually at least header field name).
- `suffix` *(string, required)* — string (usually at least line terminator).

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE

**Example.** append_urihf usage.

```opensips
append_urihf("CC-Diversion: ", "\r\n");
```

### `change_reply_status(code, reason)`

Intercept a SIP reply (in any onreply_route) and change its status code and reason phrase prior to propogating it.

**Parameters:**

- `code` *(int, required)* — Status code.
- `reason` *(string, required)* — Reason phrase.

**Usable from:** ONREPLY_ROUTE

**Example.** .

```opensips
onreply_route {
    if ($rs == "603") {
        change_reply_status(404, "Not Found");
        exit;
    }
}
```

### `codec_delete(name[, clock])`

This function can be used to delete a codec from inside an sdp payload. It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be deleted from the mapping ("a=...") and from the list of indexes ("m=..."). Returns TRUE if any deletion occurred otherwise it will return FALSE.

**Parameters:**

- `clock` *(string, optional)* — if not supplied any clockrate will match and all will be deleted. Parameter is CASE INSENSITIVE.
- `name` *(string, required)* — Parameter is CASE INSENSITIVE.

**Return codes:**

- `TRUE` — any deletion occurred
- `FALSE` — no deletion occurred

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** codec_delete usage.

```opensips
codec_delete("speex");
or
codec_delete("GSM", "8000");
```

### `codec_delete_except_re ( regexp )`

This function deletes all the codecs except those specified by the regular expression.

**Parameters:**

- `regexp` *(string, required)* — regular expression

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** will delete all codecs except PCMA and PCMU.

```opensips
codec_delete_except_re("PCMA|PCMU");#will delete all codecs except PCMA and PCMU
```

### `codec_delete_re ( regexp )`

This function has the same effect as codec_delete ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

**Parameters:**

- `regexp` *(string, required)* — POSIX regular expression

**Return codes:**

- `true` — if any deletion occurred
- `false` — otherwise

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Related:**

- `codec_delete`

**Example.** .

```opensips
codec_delete_re("PCMA|PCMU");
```

### `codec_exists (name[, clock])`

This function can be used to verify if a codec exists inside an sdp payload. It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will return TRUE otherwise it will return FALSE.

**Parameters:**

- `clock` *(string, optional)* — if not supplied any clockrate will match. Parameter is CASE INSENSITIVE.
- `name` *(string, required)* — Parameter is CASE INSENSITIVE.

**Return codes:**

- `TRUE` — codec found
- `FALSE` — codec not found

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** codec_exists usage.

```opensips
codec_exists("speex");
or
codec_exists("GSM", "8000");
```

### `codec_exists_re ( regexp )`

This function has the same effect as codec_exists ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

**Parameters:**

- `regexp` *(string, required)* — POSIX regular expression

**Return codes:**

- `TRUE` — codec found
- `FALSE` — codec not found

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Related:**

- `codec_exists`

**Example.** codec_exists_re usage.

```opensips
codec_exists_re("sp[a-z]*");
```

### `codec_move_down(name[, clock])`

This function can be used to move a codec down in the list of indexes ("m=..."). It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be moved to the back of the index list. Returns TRUE if any moves occurred otherwise it will return FALSE. The second parameter is optional, if it is not supplied any clockrate will match and all codecs will be moved to the back while preserving their original ordering. Parameters are CASE INSENSITIVE.

**Parameters:**

- `clock` *(string, optional)* — if not supplied any clockrate will match and all codecs will be moved to the back while preserving their original ordering. Parameter is CASE INSENSITIVE.
- `name` *(string, required)* — parameter is CASE INSENSITIVE.

**Return codes:**

- `TRUE` — any moves occurred
- `FALSE` — no moves occurred

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** codec_move_down usage.

```opensips
codec_move_down("speex");
or
codec_move_down("GSM", "8000");
```

**Example.** codec_move_down usage.

```opensips
/*
  This example will move speex with 8000 codec to the back of the list,
  then it will erase GSM with 8000 clock, and then it will bring all
  speex codecs to the front of the list. Speex/8000 will be behind any
  other speex.
*/
codec_move_down("speex", "8000");
codec_delete("GSM", "8000");
codec_move_up("speex");
```

### `codec_move_down_re ( regexp )`

This function has the same effect as codec_move_down ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

**Parameters:**

- `regexp` *(string, required)* — POSIX regular expression

**Return codes:**

- `true` — if any moves occurred
- `false` — otherwise

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Related:**

- `codec_move_down`

**Example.** .

```opensips
codec_move_down_re("sp[a-z]*");
```

### `codec_move_up(name[, clock])`

This function can be used to move a codec up in the list of indexes ("m=..."). It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be moved to the top of the index list. Returns TRUE if any moves occurred otherwise it will return FALSE.

**Parameters:**

- `clock` *(string, optional)* — if not supplied any clockrate will match and all codecs will be moved to the front while preserving their original ordering. Parameter is CASE INSENSITIVE.
- `name` *(string, required)* — parameter is CASE INSENSITIVE.

**Return codes:**

- `TRUE` — any moves occurred
- `FALSE` — no moves occurred

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** codec_move_up usage.

```opensips
codec_move_up("speex");
or
codec_move_up("GSM", "8000");
```

### `codec_move_up_re ( regexp )`

This function has the same effect as codec_move_up ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

**Parameters:**

- `regexp` *(string, required)* — POSIX regular expression

**Return codes:**

- `true` — if any moves occurred
- `false` — otherwise

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Related:**

- `codec_move_up`

**Example.** .

```opensips
codec_move_up_re("sp[a-z]*");
```

### `get_glob_headers_values(hdr_name_glob, hdr_names_avp, hdr_vals_avp)`

Populates the hdr_names_avp and hdr_vals_avp AVPs with all the header names and values that match the hdr_name_glob pattern.

**Parameters:**

- `hdr_name_glob` *(string, required)* — the glob pattern for matching the header names
- `hdr_names_avp` *(var, required)* — the AVP which will get populated with all the header names that match the glob pattern
- `hdr_vals_avp` *(var, required)* — the AVP which will get populated with all the header values corresponding to the header names that match the glob pattern

**Return codes:**

- `true` — at least 1 header was found that matches the glob pattern
- `false` — no match is found

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** get_glob_headers_values usage.

```opensips
if (get_glob_headers_values("X-*",$avp(names),$avp(values))) {
    xlog("All X- names are $(avp(names)[*]) and X- vals are $(avp(values)[*])\n");
}
```

### `get_updated_body_part( [mime], variable)`

This function returns into a variable the regenerated body part, meaning the body part updated with all the changes done so far by OpenSIPS. This is helpful if you want to do a sequance of operations over the body parts and some operations require to have all the previous changes applied (like first doing some codec related changes and later to rtpengine insertion). NOTE: the actual SIP message will not be affected by this operation!

**Parameters:**

- `mime` *(string, optional)* — the mime string for the body to be regenerated and returned. If missing, the whole body (with all its parts) will be regenerated.
- `variable` *(var, required)* — a variable to be used to return the regenerated body part (as text).

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Get updated SDP after codec deletion.

```opensips
...
	codec_delete_re("PCMA|PCMU");

	get_updated_body_part( "application/sdp", $var(new_sdp));

	xlog("------updated SDP is ----\\n$var(new_sdp)\\n-----------\\n");
	exit;
...
```

### `has_body_part([mime])`

The function returns true if the SIP message has any body part with the given MIME. If there is no MIME given, it will return true if at least one body part is found (with any MIME).

**Parameters:**

- `mime` *(string, optional)* — MIME type to check

**Return codes:**

- `true` — if the SIP message has any body part with the given MIME or at least one body part is found (with any MIME)

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** has_body_part usage.

```opensips
if(has_body_part("application/sdp"))
{
    # do interesting stuff here
}
```

### `has_totag()`

Check if To header field uri contains tag parameter.

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Check for To tag.

```opensips
if (has_totag()) {
	...
};
```

### `insert_hf(txt)`

Inserts 'txt' as header before the first header field.

**Parameters:**

- `txt` *(string, required)* — Header field to be inserted.

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** insert_hf usage.

```opensips
insert_hf("P-hint: VOICEMAIL\\r\\n");
insert_hf("To-username: $tU\\r\\n");
```

### `insert_hf(txt, hdr)`

Inserts 'txt' as header before first 'hdr' header field.

**Parameters:**

- `hdr` *(string, optional)* — Header name before which the 'txt' is inserted.
- `txt` *(string, required)* — Header field to be inserted.

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** insert_hf usage.

```opensips
insert_hf("P-hint: VOICEMAIL\\r\\n", "Call-ID");
insert_hf("To-username: $tU\\r\\n", "Call-ID");
```

### `is_audio_on_hold()`

The function returns true if the SIP message has an SDP body attached and at least one audio stream in on hold. The return code of the function indicates the detected hold type.

**Return codes:**

- `1` — RFC2543 hold type: null connection IP detected
- `2` — RFC3264 hold type: inactive or sendonly attributes detected

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** is_audio_on_hold usage.

```opensips
if(is_audio_on_hold())
{
    switch ($rc) {
    case 1:
        # RFC2543 hold type
    	# do interesting stuff here
        break;
    case 2:
        # RFC3264 hold type
    	# do interesting stuff here
        break;
}
```

### `is_method(name)`

Check if the method of the message matches the name. If name is a known method (invite, cancel, ack, bye, options, info, update, register, message, subscribe, notify, refer, prack), the function performs method ID testing (integer comparison) instead of ignore case string comparison.

The 'name' can be a list of methods in the form of 'method1|method2|...'. In this case, the function returns true if the SIP message's method is one from the list. IMPORTANT NOTE: in the list must be only methods defined in OpenSIPS with ID (invite, cancel, ack, bye, options, info, update, register, message, subscribe, notify, refer, prack, publish; for more see: https://www.iana.org/assignments/sip-parameters).

If used for replies, the function tests the value of method field from CSeq header.

**Parameters:**

- `name` *(string, required)* — SIP method name

**Return codes:**

- `true` — if the method of the message matches the name

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** is_method usage.

```opensips
if(is_method("INVITE"))
{
    # process INVITEs here
}
if(is_method("OPTION|UPDATE"))
{
    # process OPTIONs and UPDATEs here
}
```

### `is_present_hf(hf_name)`

Return true if a header field is present in message.

The function is also able to distinguish the compact names. For exmaple “From” will match with “f”

**Parameters:**

- `hf_name` *(string, required)* — Header field name (long or compact form).

**Return codes:**

- `true` — if a header field is present in message

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** is_present_hf usage.

```opensips
if (is_present_hf("From")) log(1, "From HF Present");
```

### `is_privacy(privacy_type)`

The function returns true if the SIP message has a Privacy header field that includes the given privacy_type among its privacy values. See https://www.iana.org/assignments/sip-parameters/sip-parameters.xhtml#sip-parameters-8 for possible privacy type values.

**Parameters:**

- `privacy_type` *(string, required)* — Privacy type value to check for in the Privacy header field.

**Return codes:**

- `true` — if the SIP message has a Privacy header field that includes the given privacy_type among its privacy values

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Check for 'id' privacy type.

```opensips
...
if(is_privacy("id"))
{
    # do interesting stuff here
}
...
```

### `is_uri_user_e164(uri)`

Checks if the username part of the given URI is an E164 number.

**Parameters:**

- `uri` *(string, required)* — a SIP URI

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, LOCAL_ROUTE

**Example.** is_uri_user_e164 usage.

```opensips
if (is_uri_user_e164($fu)) {  # Check From header URI user part
   ...
}
if (is_uri_user_e164($avp(uri)) {
   # Check user part of URI stored in avp uri
   ...
};
```

### `list_hdr_add_option(hdr_name, option)`

Add a new option/token at the end of the list in the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Disposition headers) body format

**Parameters:**

- `hdr_name` *(string, required)* — the name of the header where the option has to be added. If multiple instances of that header are present in the SIP message, the add will be performed on the first instance. Any kind of header name is supported - RFC3261 standard, RFC extensions or custom names.
- `opt` *(string, required)* — the option/token to be added to the CSV list. Note there is not verification for duplicated (if the newly added option is not already present in the header).

**Return codes:**

- `true` — option successfully added
- `false` — no header was found or parsing/runtime error

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** add 100rel for advertising.

```opensips
if (!list_hdr_has_option("Supported", "100rel"))
    list_hdr_add_option("Supported", "100rel");
```

### `list_hdr_has_option(hdr_name, option)`

Checks and returns true if the given option/token is listed in the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Dispsition headers) body format

**Parameters:**

- `hdr_name` *(string, required)* — the name of the header to be checked. Note that all instances of that header will be checked (if the header has multiple instances in the SIP message). Any kind of header name is supported - RFC3261 standard, RFC extensions or custom names.
- `opt` *(string, required)* — the option/tolen to be searched for.

**Return codes:**

- `true` — option found listed in one of the header instances
- `false` — no header was found, option was not found or parsing/runtime error

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** check if 100rel is advertised.

```opensips
if (list_hdr_has_option("Supported", "100rel"))
	xlog("100rel option found\n");
```

### `list_hdr_remove_option(hdr_name, option)`

Removes an option/token from the list inside the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Dispsition headers) body format

**Parameters:**

- `hdr_name` *(string, required)* — the name of the header where the option has to be removed from. If the option is duplicated in the same header, only the last one will be removed. If multiple instances of that header are present in the SIP message, the remove will be performed on all instance instance. Any kind of header name is supported - RFC3261 standard, RFC extensions or custom names.
- `opt` *(string, required)* — the option/token to be removed from the CSV list. Note that if this the only option in the header, the whole header will be removed.

**Return codes:**

- `true` — option successfully removed from at least one header instance
- `false` — no header was found, token was not found or parsing/runtime error

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** add 100rel for advertising.

```opensips
if (list_hdr_has_option("Supported", "100rel"))
    list_hdr_remove_option("Supported", "100rel");
list_hdr_add_option("Supported", "optionX");
```

### `remove_body_part([mime[, revert]])`

Removes from the message body all the body parts with the given mime. The necessary corrections over the Content-Type and Content-Length headers are automatically done. If a MIME type is given, it will delete only the body parts with that mime. If no MIME given, all the parts (entire body) will be removed.

**Parameters:**

- `mime` *(string, optional)* — MIME type to be checked against the body parts; If not given, all parts are to remvoed;
- `revert` *(string, optional)* — useful only if a MIME was specified. If "revert" string is given here, the function will delete all body parts but the ones with the given MIME.
  - `revert`

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Usage examples for removing body parts.

```opensips
...
# delete entire body message (all parts)
remove_body_part();
# delete all body parts with mime "application/isup"
remove_body_part("application/isup");
# delete all body parts but keep the the ones with  "application/sdp"
remove_body_part("application/sdp","revert")
...
```

### `remove_hf(hname)`

Remove from message all headers with name “hname”

**Parameters:**

- `hname` *(string, required)* — header name to be removed.

**Return codes:**

- `true` — if at least one header is found and removed

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** remove_hf usage.

```opensips
if(remove_hf("User-Agent"))
{
    # User Agent header removed
}
```

### `remove_hf_glob(hname_pattern)`

Remove from message all headers matching the “hname\_pattern” glob pattern.

**Parameters:**

- `hname_pattern` *(string, required)* — glob pattern

**Return codes:**

- `true` — at least one header is found and removed

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** removes X-Billing-Account, X-Billing-Price, X-Billing-rateplan, etc.

```opensips
remove_hf_glob("X-Billing\*");
```

### `remove_hf_re(hname_expr)`

Remove from message all headers matching the “hname\_expr” POSIX regular expression.

**Parameters:**

- `hname_expr` *(string, required)* — regular expression

**Return codes:**

- `true` — at least one header is found and removed

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Removes headers matching the regex.

```opensips
remove_hf_re("^X-g.+\[0-9\]");
```

### `ruri_add_param(param)`

Add to RURI an URI parameter formated as "name=value".

**Parameters:**

- `param` *(string, required)* — parameter to be appended in “name=value” format

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Add parameter nat=yes to RURI.

```opensips
ruri_add_param("nat=yes");
```

### `ruri_del_param(param)`

Delete a parameter, its value and any leading ";" from the Request-URI of the current SIP message.

**Parameters:**

- `param` *(string, required)* — the parameter to be removed

**Return codes:**

- `1` — on a successful deletion
- `-1` — otherwise

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** ruri_del_param usage.

```opensips
ruri_del_param("user");
```

### `ruri_has_param(param[,value])`

Find if Request URI has a given parameter. If no value is given, the function will look for the paramter with no value, oherwise it will search for the parameter with the matching value.

**Parameters:**

- `param` *(string, required)* — parameter name to look for
- `value` *(string, optional)* — parameter value to match

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** Check if RURI has parameter 'user' with value 'phone'.

```opensips
if (ruri_has_param("user","phone")) {
	...
};
```

### `ruri_tel2sip()`

Converts RURI, if it is tel URI, to SIP URI. Returns true, only if conversion succeeded or if no conversion was needed (like RURI was not tel URI.

**Return codes:**

- `true` — if conversion succeeded or if no conversion was needed (like RURI was not tel URI)

**Usable from:** REQUEST_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** ruri_tel2sip usage.

```opensips
ruri_tel2sip();
```

### `sip_to_json(out_var)`

Returns a JSON formatted representation of the current SIP message, containing first_line , headers and body json members Useful in cases when you want to pass a generic SIP message to a SIP agnostic entity, but still want to provide some layer of SIP parsing before sending the full message further.

**Parameters:**

- `out_var` *(string, required)* — the output JSON formatted SIP message variable

**Return codes:**

- `true` — success
- `false` — failure

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** sip_to_json usage.

```opensips
if (sip_to_json($var(out_sip_json))) {
    xlog("The JSON format for the current SIP message is $var(out_sip_json) \n");
}
```

### `sipmsg_validate([flags[, result_pvar]])`

The function returns true if the SIP message is properly built according to SIP RFC3261. It verifies if the mandatory headers for each request/reply and can also check the format of the headers body.

**Parameters:**

- `flags` *(string, optional)* — Optional flags to control validation checks.
  - `s`
  - `h`
  - `m`
  - `r`
  - `f`
  - `t`
  - `c`
- `result_pvar` *(pvar, optional)* — Sets resulting pvar with text error reason in case of negative result ( easy for logging or propagating the rejection reason back to the bogus UA )

**Return codes:**

- `1` — the message is RFC3261 compliant and has been successfully validated
- `-1` — No SIP message
- `-2` — Header Parsing error
- `-3` — No Call-ID header
- `-4` — No Content-Length header for transports that require it ( eg. TCP )
- `-5` — Invalid Content-Length, other from the size of the actual body
- `-6` — SDP body parsing error
- `-7` — No Cseq header
- `-8` — No From header
- `-9` — No To header
- `-10` — No Via header
- `-11` — Request URI parse error
- `-12` — Bad hostname in R-URI
- `-13` — No Max-Forwards header
- `-14` — No Contact header
- `-15` — Path user for non-Register request
- `-16` — No allow header in 405 reply
- `-17` — No Min-Expire header in 423 reply
- `-18` — No Proxy-Authorize header in 407 reply
- `-19` — No Unsupported header in 420 reply
- `-20` — No WWW-Authorize header in 401 reply
- `-21` — No Content-Type header
- `-22` — To header parse error
- `-23` — Bad hostname in To header
- `-24` — From header parse error
- `-25` — Bad hostname in From header
- `-26` — Contact header parse error
- `-27` — Bad URI username
- `-28` — Bad From URI username
- `-29` — Bad To URI username
- `-30` — Contact header contains * for non-Register request
- `-255` — undefined errors

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE

**Example.** Basic validation.

```opensips
...
if(!sipmsg_validate())
{
	send_reply(400, "Bad Request");
	exit;
}
...
```

**Example.** Validation with SDP and header body checks.

```opensips
...
# checks also the SDP and headers body
if(!sipmsg_validate("sh", $var(err_reason)))
{
	send_reply(400, "Bad Request/Body");
	exit;
}
...
```

**Example.** Check Contact header for invalid star.

```opensips
...
# checks Contact header for a 200 Ok reply and logs when it's *
if(!sipmsg_validate("c"))
{
	if ($rc == -30)
		xlog("Invalid * Contact header found\\n");
}
...
```

### `stream_delete(regexp[,regexp2])`

This function can be used to delete a whole stream from inside an sdp payload. It will search for the stream inside all sdp sessions. If it is found anywhere it will be deleted along with all attributes Returns TRUE if any deletion occurred otherwise it will return FALSE.

**Parameters:**

- `regexp` *(string, required)* — a POSIX regular expression to match the stream media name.
- `regexp2` *(string, optional)* — an optional POSIX regular expression to match the stream transport name.

**Return codes:**

- `true` — deletion occurred
- `false` — no deletion occurred

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** prevent usage of video.

```opensips
stream_delete("video");
```

### `stream_exists(regexp[,regexp2])`

This function can be used to verify if a stream exists inside an sdp payload. It will search for the stream inside all sdp sessions. If it is found anywhere it will return TRUE otherwise it will return FALSE.

**Parameters:**

- `regexp` *(string, required)* — a POSIX regular expression to match the stream media name.
- `regexp2` *(string, optional)* — an optional POSIX regular expression to match the stream transport name.

**Return codes:**

- `true` — stream found
- `false` — stream not found

**Usable from:** REQUEST_ROUTE, ONREPLY_ROUTE, FAILURE_ROUTE, BRANCH_ROUTE, LOCAL_ROUTE

**Example.** check for FAX.

```opensips
stream_exists("image");
```

**Example.** .

```opensips
stream_exists("audio","SAVP");
```

## Configuration Examples

### Example 1.1. `append_to_reply` usage

Append 'txt' as header to all replies that will be generated by OpenSIPS for this request.

```opensips
...
append_to_reply("Foo: bar\r\n");
append_to_reply("Foo: $rm at $Ts\r\n");
...
```
### Example 1.2. `append_to_reply` usage

Append 'txt' as body to all replies that will be generated by OpenSIPS for this request.

```opensips
...
append_body_to_reply( $var(sdp_body) );
...
```
### Example 1.3. `append_hf` usage

Appends 'txt' as header after the last header field. If 'hdr_anchor' is given, 'txt' will be appended after the first occurrence of 'hdr_anchor' instead.

```opensips
...
append_hf("P-hint: VOICEMAIL\r\n");
append_hf("From-username: $fU\r\n");
append_hf("From-username: $fU\r\n", "Call-ID");
...
```
### Example 1.4. `insert_hf` usage

Inserts 'txt' as header before the first header field.

```opensips
...
insert_hf("P-hint: VOICEMAIL\r\n");
insert_hf("To-username: $tU\r\n");
...
```
### Example 1.5. `insert_hf` usage

Inserts 'txt' as header before first 'hdr' header field.

```opensips
...
insert_hf("P-hint: VOICEMAIL\r\n", "Call-ID");
insert_hf("To-username: $tU\r\n", "Call-ID");
...
```
### Example 1.6. `append_urihf` usage

Append header field name with original Request-URI in middle.

```opensips
...
append_urihf("CC-Diversion: ", "\r\n");
...
```
### Example 1.7. `is_present_hf` usage

Return true if a header field is present in message.

```opensips
...
if (is_present_hf("From")) log(1, "From HF Present");
...
```
### Example 1.8. `append_time` usage

Adds a time header to the reply of the request. You must use it before functions that are likely to send a reply, e.g., save() from 'registrar' module. Header format is: “Date: %a, %d %b %Y %H:%M:%S GMT”, with the legend: ...

```opensips
...
append_time();
...
```
### Example 1.9. `is_method` usage

Check if the method of the message matches the name. If name is a known method (invite, cancel, ack, bye, options, info, update, register, message, subscribe, notify, refer, prack), the function performs method ID testing (integer comparison) instead of ignore case string comparison.

```opensips
...
if(is_method("INVITE"))
{
    # process INVITEs here
}
if(is_method("OPTION|UPDATE"))
{
    # process OPTIONs and UPDATEs here
}
...
```
### Example 1.10. `remove_hf` usage

Remove from message all headers with name “hname”

```opensips
...
if(remove_hf("User-Agent"))
{
    # User Agent header removed
}
...
```
### Example 1.11. `remove_hf_re` usage

Remove from message all headers matching the “hname_expr” POSIX regular expression.

```opensips
...
remove_hf_re("^X-g.+[0-9]");
...
```
### Example 1.12. `remove_hf_glob` usage

Remove from message all headers matching the “hname_pattern” glob pattern.

```opensips
...
# removes X-Billing-Account, X-Billing-Price, X-Billing-rateplan, etc
remove_hf_glob("X-Billing*");
...
```
### Example 1.13. `has_totag` usage

Check if To header field uri contains tag parameter.

```opensips
...
if (has_totag()) {
	...
};
...
```
### Example 1.14. `ruri_has_param` usage

Find if Request URI has a given parameter. If no value is given, the function will look for the paramter with no value, oherwise it will search for the parameter with the matching value.

```opensips
...
if (ruri_has_param("user","phone")) {
	...
};
...
```
### Example 1.15. `ruri_add_param` usage

Add to RURI an URI parameter formated as "name=value".

```opensips
...
ruri_add_param("nat=yes");
...
```
### Example 1.16. `ruri_del_param` usage

Delete a parameter, its value and any leading ";" from the Request-URI of the current SIP message.

```opensips
...
ruri_del_param("user");
...
```
### Example 1.17. `ruri_tel2sip` usage

Converts RURI, if it is tel URI, to SIP URI. Returns true, only if conversion succeeded or if no conversion was needed (like RURI was not tel URI.

```opensips
...
ruri_tel2sip();
...
```
### Example 1.18. `is_uri_user_e164` usage

Checks if the username part of the given URI is an E164 number.

```opensips
...
if (is_uri_user_e164($fu)) {  # Check From header URI user part
   ...
}
if (is_uri_user_e164($avp(uri)) {
   # Check user part of URI stored in avp uri
   ...
};
...
```
### Example 1.19. `has_body_part` usage

The function returns _true_ if the SIP message has any body part with the given MIME. If there is no MIME given, it will return true if at least one body part is found (with any MIME).

```opensips
...
if(has_body_part("application/sdp"))
{
    # do interesting stuff here
}
...
```
### Example 1.20. `is_audio_on_hold` usage

The function returns _true_ if the SIP message has an SDP body attached and at least one audio stream in on hold. The return code of the function indicates the detected hold type:

```opensips
...
if(is_audio_on_hold())
{
    switch ($rc) {
    case 1:
        # RFC2543 hold type
    	# do interesting stuff here
        break;
    case 2:
        # RFC3264 hold type
    	# do interesting stuff here
        break;
}
...
```
### Example 1.21. `is_privacy` usage

The function returns _true_ if the SIP message has a Privacy header field that includes the given privacy_type among its privacy values. See https://www.iana.org/assignments/sip-parameters/sip-parameters.xhtml#sip-parameters-8 for possible privacy type values.

```opensips
...
if(is_privacy("id"))
{
    # do interesting stuff here
}
...
```
### Example 1.22. `remove_body_part()` usage

Removes from the message body all the body parts with the given mime. The necessary corrections over the Content-Type and Content-Length headers are automatically done.

```opensips
...
# delete entire body message (all parts)
remove_body_part();
# delete all body parts with mime "application/isup"
remove_body_part("application/isup");
# delete all body parts but keep the the ones with  "application/sdp"
remove_body_part("application/sdp","revert")
...
```
### Example 1.23. `add_body_part` usage

This function can be used to add a new body part to the message body. If another part already exist, body of the message will be converted to a multi-part body automatically.

```opensips
...
add_body_part("Hello World!", "text/plain");
...
```
### Example 1.24. `get_updated_body_part` usage

This function returns into a variable the regenerated body part, meaning the body part updated with all the changes done so far by OpenSIPS. This is helpful if you want to do a sequance of operations over the body parts and some operations require to have all the previous changes applied (like first doing some codec related changes and later to rtpengine insertion).

```opensips
...
	codec_delete_re("PCMA|PCMU");

	get_updated_body_part( "application/sdp", $var(new_sdp));

	xlog("------updated SDP is ----\n$var(new_sdp)\n-----------\n");
	exit;
...
```
### Example 1.25. `sipmsg_validate` usage

The function returns _true_ if the SIP message is properly built according to SIP RFC3261. It verifies if the mandatory headers for each request/reply and can also check the format of the headers body.

```opensips
...
if(!sipmsg_validate())
{
	send_reply(400, "Bad Request");
	exit;
}
...

...
# checks also the SDP and headers body
if(!sipmsg_validate("sh", $var(err_reason)))
{
	send_reply(400, "Bad Request/Body");
	exit;
}
...

...
# checks Contact header for a 200 Ok reply and logs when it's *
if(!sipmsg_validate("c"))
{
	if ($rc == -30)
		xlog("Invalid * Contact header found\n");
}
...
```
### Example 1.26. `codec_exists` usage

This function can be used to verify if a codec exists inside an sdp payload. It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will return TRUE otherwise it will return FALSE.

```opensips
...
codec_exists("speex");
or
codec_exists("GSM", "8000");
...
```
### Example 1.27. `codec_delete` usage

This function can be used to delete a codec from inside an sdp payload. It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be deleted from the mapping ("a=...") and from the list of indexes ("m=..."). Returns TRUE if any deletion occurred otherwise it will return FALSE.

```opensips
...
codec_delete("speex");
or
codec_delete("GSM", "8000");
...
```
### Example 1.28. `codec_move_up` usage

This function can be used to move a codec up in the list of indexes ("m=..."). It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be moved to the top of the index list. Returns TRUE if any moves occurred otherwise it will return FALSE.

```opensips
...
codec_move_up("speex");
or
codec_move_up("GSM", "8000");
...
```
### Example 1.29. `codec_move_down` usage

This function can be used to move a codec down in the list of indexes ("m=..."). It will search for the codec inside all streams from all sdp sessions. If it is found anywhere it will be moved to the back of the index list. Returns TRUE if any moves occurred otherwise it will return FALSE. The second parameter is optional, if it is not supplied any clockrate will match and all codecs will be moved to the back while preserving their original ordering. Parameters are CASE INSENSITIVE.

```opensips
...
codec_move_down("speex");
or
codec_move_down("GSM", "8000");
...
```
### Example 1.30. `codec_move_down` usage

This example will move speex with 8000 codec to the back of the list, then it will erase GSM with 8000 clock, and then it will bring all speex codecs to the front of the list. Speex/8000 will be behind any other speex.

```opensips
/*
  This example will move speex with 8000 codec to the back of the list,
  then it will erase GSM with 8000 clock, and then it will bring all
  speex codecs to the front of the list. Speex/8000 will be behind any
  other speex.
*/
codec_move_down("speex", "8000");
codec_delete("GSM", "8000");
codec_move_up("speex");
...
```
### Example 1.31. `codec_exists_re` usage

This function has the same effect as codec_exists ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

```opensips
...
codec_exists_re("sp\[a-z\]*");
...
```
### Example 1.32. `codec_delete_re` usage

This function has the same effect as codec_delete ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

```opensips
...
codec_delete_re("PCMA|PCMU");
...
```
### Example 1.33. `codec_delete_except_re` usage

This function deletes all the codecs except those specified by the regular expression.

```opensips
...
codec_delete_except_re("PCMA|PCMU");#will delete all codecs except PCMA and PCMU
...
```
### Example 1.34. `codec_move_up_re` usage

This function has the same effect as codec_move_up ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

```opensips
...
codec_move_up_re("sp\[a-z\]*");
...
```
### Example 1.35. `codec_move_down_re` usage

This function has the same effect as codec_move_down ( without the clock parameter ) the only difference is that it takes a POSIX regular expression as a parameter.

```opensips
...
codec_move_down_re("sp\[a-z\]*");
...
```
### Example 1.36. `codec_move_down` usage

This example will move speex with 8000 codec to the back of the list, then it will erase GSM with 8000 clock, and then it will bring all speex codecs to the front of the list. Speex/8000 will be behind any other speex.

```opensips
/*
  This example will move speex with 8000 codec to the back of the list,
  then it will erase GSM with 8000 clock, and then it will bring all
  speex codecs to the front of the list. Speex/8000 will be behind any
  other speex.
*/
codec_move_down("speex","8000");
codec_delete("GSM","8000");
codec_move_up("speex");
...
```
### Example 1.37. `change_reply_status` usage

Intercept a SIP reply (in any onreply_route) and change its status code and reason phrase prior to propogating it.

```opensips
...
onreply_route {
    if ($rs == "603") {
        change_reply_status(404, "Not Found");
        exit;
    }
}
...
```
### Example 1.38. `stream_exists` usage

This function can be used to verify if a stream exists inside an sdp payload. It will search for the stream inside all sdp sessions. If it is found anywhere it will return TRUE otherwise it will return FALSE.

```opensips
...
# check for FAX
stream_exists("image");
...
stream_exists("audio","SAVP");
...
```
### Example 1.39. `stream_delete` usage

This function can be used to delete a whole stream from inside an sdp payload. It will search for the stream inside all sdp sessions. If it is found anywhere it will be deleted along with all attributes Returns TRUE if any deletion occurred otherwise it will return FALSE.

```opensips
...
# prevent usage of video
stream_delete("video");
...
```
### Example 1.40. `list_hdr_has_option` usage

Checks and returns true if the given option/token is listed in the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Dispsition headers) body format

```opensips
...
# check if 100rel is advertised
if (list_hdr_has_option("Supported", "100rel"))
	xlog("100rel option found\n");
...
```
### Example 1.41. `list_hdr_add_option` usage

Add a new option/token at the end of the list in the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Disposition headers) body format

```opensips
...
# add 100rel for advertising
if (!list_hdr_has_option("Supported", "100rel"))
    list_hdr_add_option("Supported", "100rel");

```
### Example 1.42. `list_hdr_remove_option` usage

Removes an option/token from the list inside the body of the given header. The header must have its body formated as a CSV list of tokens/option (like the Supported, Require, Content-Dispsition headers) body format

```opensips
...
# add 100rel for advertising
if (list_hdr_has_option("Supported", "100rel"))
    list_hdr_remove_option("Supported", "100rel");
list_hdr_add_option("Supported", "optionX");

```
### Example 1.43. `get_glob_headers_values` usage

Populates the hdr_names_avp and hdr_vals_avp AVPs with all the header names and values that match the hdr_name_glob pattern.

```opensips
...
       if (get_glob_headers_values("X-*",$avp(names),$avp(values))) {
           xlog("All X- names are $(avp(names)[*]) and X- vals are $(avp(values)[*])\n");
       }
...
```
### Example 1.44. `sip_to_json` usage

Returns a JSON formatted representation of the current SIP message, containing first_line , headers and body json members Useful in cases when you want to pass a generic SIP message to a SIP agnostic entity, but still want to provide some layer of SIP parsing before sending the full message further.

```opensips
...
       if (sip_to_json($var(out_sip_json))) {
           xlog("The JSON format for the current SIP message is $var(out_sip_json) \n");
       }
...
```
