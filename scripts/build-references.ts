/**
 * opensips-skills build entry point (stub).
 *
 * Real implementation arrives in milestone 1 (schema validation path) and
 * milestone 2 (full orchestrator with CLI, dry-run, error handling).
 * See `docs/plan/01-schema-mirroring-and-validation.md` and
 * `docs/plan/02-core-pipeline-foundation.md`.
 *
 * This stub exists so M0's acceptance commands (`npx tsc --noEmit`,
 * `npm run lint`) have at least one TypeScript file to operate on.
 */

export {};
