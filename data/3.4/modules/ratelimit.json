{
  "id": "module-ratelimit-3.4",
  "document_type": "module",
  "version": "3.4",
  "title": "ratelimit Module",
  "url": "https://opensips.org/html/docs/modules/3.4.x/ratelimit.html",
  "content_markdown": "This module implements rate limiting for SIP requests. In contrast to the PIKE module this limits the flow based on a per SIP request type basis and not per source IP. The latest sources allow you to dynamically group several messages into some entities and limit the traffic based on them. The MI interface can be used to change tunables while running OpenSIPS.\n\nThis module is integrated with the OpenSIPS Key-Value Interface, providing support for distributed rate limiting using Redis or Memcached CacheDB backends. The internal limiting data will no longer be kept on each OpenSIPS instance. It will be stored in the distributed Key-Value database and queried by each instance before deciding if a SIP message should be blocked or not.\n\nTo achieve a distributed ratelimit feature, the module can also replicate its pipes counters to different OpenSIPS instances using the clusterer module. To do that, define the _pipe_replication_cluster_ parameter in your configuration script.\n\nStarting with OpenSIPS 3.2, choosing whether to replicate a pipe over CacheDB backends or bin replication is triggered by the flags specified when the pipe is created: adding the _/r_ suffix to the pipe's name will replicate through CacheDB, and adding _/b_ will replicate through bin/clusterer.",
  "category": "Modules",
  "extracted_at": "2026-04-25T09:45:58.700Z",
  "module_name": "ratelimit",
  "overview": "This module implements rate limiting for SIP requests. In contrast to the PIKE module this limits the flow based on a per SIP request type basis and not per source IP. The latest sources allow you to dynamically group several messages into some entities and limit the traffic based on them. The MI interface can be used to change tunables while running OpenSIPS.\n\nThis module is integrated with the OpenSIPS Key-Value Interface, providing support for distributed rate limiting using Redis or Memcached CacheDB backends. The internal limiting data will no longer be kept on each OpenSIPS instance. It will be stored in the distributed Key-Value database and queried by each instance before deciding if a SIP message should be blocked or not.\n\nTo achieve a distributed ratelimit feature, the module can also replicate its pipes counters to different OpenSIPS instances using the clusterer module. To do that, define the _pipe_replication_cluster_ parameter in your configuration script.\n\nStarting with OpenSIPS 3.2, choosing whether to replicate a pipe over CacheDB backends or bin replication is triggered by the flags specified when the pipe is created: adding the _/r_ suffix to the pipe's name will replicate through CacheDB, and adding _/b_ will replicate through bin/clusterer.",
  "how_it_works": "Limiting the rate messages are processed on a system directly influences the load. The ratelimit module can be used to protect a single host or to protect an OpenSIPS cluster when run on the dispatching box in front.\n\nDistributed limiting is useful when the rate limit should be performed not only on a specific node, but on the entire platform.\n\nNOTE: that this behavior only makes sense when the pipe algorithm used is TAILDROP or RED.\n\nA sample configuration snippet might look like this:\n\n...\n\tif (!rl_check($rU, 50, \"TAILDROP\")) {\n\t\tsl_send_reply(503, \"Server Unavailable\");\n\t\texit;\n\t};\n...\n\t\n\nUpon every incoming request listed above rl_check is invoked and the entity identified by the R-URI user is checked. It returns an OK code if the current per request load is below the configured threshold. If the load is exceeded the function returns an error and an administrator can discard requests with a stateless response.\n\n1.3. Static Rate Limiting Algorithms\n\nThe ratelimit module supports two different static algorithms to be used by rl_check to determine whether a message should be blocked or not.\n\n1.3.1. Tail Drop Algorithm (TAILDROP)\n\nThis is a trivial algorithm that imposes some risks when used in conjunction with long timer intervals. At the start of each interval an internal counter is reset and incremented for each incoming message. Once the counter hits the configured limit rl_check returns an error.\n\nThe downside of this algorithm is that it can lead to SIP client synchronization. During a relatively long interval only the first requests (i.e. REGISTERs) would make it through. Following messages (i.e. RE-REGISTERs) will all hit the SIP proxy at the same time when a common Expire timer expired. Other requests will be retransmissed after given time, the same on all devices with the same firmware/by the same vendor.\n\n1.3.2. Random Early Detection Algorithm (RED)\n\nRandom Early Detection tries to circumvent the synchronization problem imposed by the tail drop algorithm by measuring the average load and adapting the drop rate dynamically. When running with the RED algorithm OpenSIPS will return errors to the OpenSIPS routing engine every n'th packet trying to evenly spread the measured load of the last timer interval onto the current interval. As a negative side effect OpenSIPS might drop messages although the limit might not be reached within the interval. Decrease the timer interval if you encounter this.\n\n1.3.3. Slot Based Taildropping (SBT)\n\nSBT holds a window consisting of one or more slots. You can set the _window_size_ parameter(seconds) which means for how long we should look back to count the calls and _slot_period_ parameter(miliseconds) which tells how granular the algorithm should be. The number of slots will be _window_size_/_slot_period_. If, for example, you have _window_size_=_slot_period_=1 second, then after each second you shall lose the call count, but if you set the _slot_period_ to 100 milliseconds, then when your call will be outside the window, the calls in the first 100 milliseconds shall be dropped, and the rest in the next 900 shall be kept.\n\n1.3.4. Network Algorithm (NETWORK)\n\nThis algorithm relies on information provided by network interfaces. The total amount of bytes waiting to be consumed on all the network interfaces is retrieved once every timer_interval seconds. If the returned amount exceeds the limit specified in the modparam, rl_check returns an error.\n\n1.4. Dynamic Rate Limiting Algorithms\n\nWhen running OpenSIPS on different machines, one has to adjust the drop rates for the static algorithms to maintain a sub 100% load average or packets start getting dropped in the network stack. While this is not in itself difficult, it isn't neither accurate nor trivial: another server taking a notable fraction of the cpu time will require re-tuning the parameters.\n\nWhile tuning the drop rates from the outside based on a certain factor is possible, having the algorithm run inside ratelimit permits tuning the rates based on internal server parameters and is somewhat more flexible (or it will be when support for external load factors - as opposed to cpu load - is added).\n\n1.4.1. Feedback Algorithm (FEEDBACK)\n\nUsing the PID Controller model (see Wikipedia page), the drop rate is adjusted dynamically based on the load factor so that the load factor always drifts towards the specified limit (or setpoint, in PID terms).\n\nAs reading the cpu load average is relatively expensive (opening /proc/stat, parsing it, etc), this only happens once every timer_interval seconds and consequently the FEEDBACK value is only at these intervals recomputed. This in turn makes it difficult for the drop rate to adjust quickly. Worst case scenarios are request rates going up/down instantly by thousands - it takes up to 20 seconds for the controller to adapt to the new request rate.\n\nGenerally though, as real life request rates drift by less, adapting should happen much faster.\n\nIMPORTANT NOTE: as this algorithm is diven by the load factor, the values for the limits must be between 0 and 100 (as percentages) and the limits for all the checks and pipes must be the same (only one value). Again, this limitation are specific to this algorithm and not to the implementation.",
  "dependencies_required": null,
  "dependencies_optional": null,
  "exported_parameters": [
    {
      "name": "timer_interval",
      "type": "integer",
      "default_value": "10",
      "description": "The timer interval in seconds when the Network and Feedback algorithms run their queries, and the other algorithms reset their counters.\n\nIMPORTANT: A too small value may lead to performance penalties due to timer process overloading.",
      "example_value": "5",
      "example_code": "modparam(\"ratelimit\", \"timer\\_interval\", 5)"
    },
    {
      "name": "limit_per_interval",
      "type": "integer",
      "default_value": "0(limit per-second)",
      "description": "This parameter configures the way that a pipe's limit is specified in the _rl\\_check_ function and only affects the Taildrop and RED algorithms. A value of 1 means that the limit is set per-_timer\\_interval_ while a value of 0 means per-second.",
      "possible_values": [
        "0",
        "1"
      ],
      "example_value": "1",
      "example_code": "modparam(\"ratelimit\", \"limit\\_per\\_interval\", 1)"
    },
    {
      "name": "expire_time",
      "type": "integer",
      "default_value": "3600",
      "description": "This parameter specifies how long a pipe should be kept in memory after it becomes idle (no more operations are performed on the pipe) until deleted.",
      "example_value": "1800",
      "example_code": "modparam(\"ratelimit\", \"expire\\_time\", 1800)"
    },
    {
      "name": "hash_size",
      "type": "integer",
      "default_value": "1024",
      "description": "The size of the hash table internally used to keep the pipes. A larger table is much faster but consumes more memory. The hash size must be a power of 2 number.",
      "example_value": "512",
      "example_code": "modparam(\"ratelimit\", \"hash\\_size\", 512)"
    },
    {
      "name": "default_algorithm",
      "type": "string",
      "default_value": "\"TAILDROP\"",
      "description": "Specifies which algorithm should be assumed in case it isn't explicitly specified in the _rl\\_check_ function.",
      "example_value": "\"RED\"",
      "example_code": "modparam(\"ratelimit\", \"default\\_algorithm\", \"RED\")"
    },
    {
      "name": "cachedb_url",
      "type": "string",
      "default_value": "disabled",
      "description": "Enables distributed rate limiting and specifies the backend that should be used by the CacheDB interface.",
      "example_value": "redis://root:root@127.0.0.1/",
      "example_code": "modparam(\"ratelimit\", \"cachedb\\_url\", \"redis://root:root@127.0.0.1/\")"
    },
    {
      "name": "db_prefix",
      "type": "string",
      "default_value": "rl_pipe_",
      "description": "Specifies what prefix should be added to the pipe name. This is only used when distributed rate limiting is enabled.",
      "example_value": "ratelimit_",
      "example_code": "modparam(\"ratelimit\", \"db\\_prefix\", \"ratelimit\\_\")"
    },
    {
      "name": "repl_buffer_threshold",
      "type": "string",
      "default_value": "32767 bytes",
      "description": "Used to specify the length of the buffer used by the binary replication, in bytes, when a flush should be performed - the pipes gathered until then should be sent on the network. This is used to avoid using large amount of memory for pipes replication.",
      "example_value": "500",
      "example_code": "modparam(\"ratelimit\", \"repl\\_buffer\\_threshold\", 500)"
    },
    {
      "name": "repl_timer_interval",
      "type": "string",
      "default_value": "200 ms",
      "description": "Timer in milliseconds, used to specify how often the module should replicate its counters to the other instances.",
      "example_value": "100",
      "example_code": "modparam(\"ratelimit\", \"repl\\_timer\\_interval\", 100)"
    },
    {
      "name": "repl_timer_expire",
      "type": "string",
      "default_value": "10 s",
      "description": "Timer in seconds, used to specify when the counter received from a different instance should no longer be taken into account. This is used to prevent obsolete values, in case an instance stops replicating its counters.",
      "example_value": "10",
      "example_code": "modparam(\"ratelimit\", \"repl\\_timer\\_expire\", 10)"
    },
    {
      "name": "pipe_replication_cluster",
      "type": "integer",
      "default_value": "0",
      "description": "Specifies the cluster ID where pipes will be replicated to and received from.",
      "example_value": "1",
      "example_code": "modparam(\"ratelimit\", \"pipe\\_replication\\_cluster\", 1)"
    },
    {
      "name": "window_size",
      "type": "int",
      "default_value": "“10”",
      "description": "How long the history in SBT should be in seconds.",
      "example_value": "5",
      "example_code": "modparam(\"ratelimit\", \"window\\_size\", 5)"
    },
    {
      "name": "slot_period",
      "type": "int",
      "default_value": "“200”",
      "description": "Value of one slot in milliseconds. This parameter determines how granular the algorithm should be. The number of slots will be determined by window\\_size/slot\\_period.",
      "example_value": "100",
      "example_code": "modparam(\"ratelimit\", \"window\\_size\", 5)\n#we will have 50 slots of 100 milliseconds\nmodparam(\"ratelimit\", \"slot\\_period\", 100)"
    }
  ],
  "exported_functions": [
    {
      "name": "rl_check",
      "signature": "rl_check(name, limit[, algorithm])",
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "this is the name that identifies the pipe which should be checked. One can also specify the /s suffix to indicate the pipe should be replicated over cached, or /b to replicate over bin/clusterer interface.",
          "valid_values": [
            "/s",
            "/b"
          ]
        },
        {
          "name": "limit",
          "type": "int",
          "required": true,
          "description": "this specifies the threshold limit of the pipe. It is strongly related to the algorithm used. Note that the limit should be specified as per-second, not per-timer_interval."
        },
        {
          "name": "algorithm",
          "type": "string",
          "required": false,
          "description": "this parameter reffers to the algorithm used to check the pipe. If it is not set, the default value is used."
        }
      ],
      "return_type": "int",
      "return_values": [
        {
          "value": "error code",
          "condition": "if the limit for the matched pipe is reached"
        }
      ],
      "description": "Check the current request against the pipe identified by name and changes/updates the limit. If no pipe is found, then a new one is created with the specified limit and algorithm, if specified. If the algorithm parameter doesn't exist, the default one is used.\n\nNOTE: A pipe's algorithm cannot be dynamically changed. Only the one specified when the pipe was created will be considered.\n\nNOTE: This function increments the pipe's counter every time it is called, even if the call should be declined. Therefore If you are using ratelimit to limit only successful traffic, you need to explicitely decrease the counter for the declined calls using the rl_dec_count() function.",
      "usage_context": [
        "REQUEST_ROUTE",
        "FAILURE_ROUTE",
        "ONREPLY_ROUTE",
        "BRANCH_ROUTE",
        "ERROR_ROUTE",
        "LOCAL_ROUTE",
        "TIMER_ROUTE",
        "EVENT_ROUTE"
      ],
      "examples": [
        {
          "language": "opensips",
          "code": "\t# perform a pipe match for all INVITE methods using RED algorithm\n\tif (is_method(\"INVITE\")) {\n\t\tif (!rl_check(\"pipe_INVITE\", 100, \"RED\")) {\n\t\t\tsl_send_reply(503, \"Server Unavailable\");\n\t\t\texit;\n\t\t};\n\t};\n...\n\t# use default algorithm for each different gateway\n\t$var(limit) = 10;\n\tif (!rl_check(\"gw_$ru\", $var(limit))) {\n\t\tsl_send_reply(503, \"Server Unavailable\");\n\t\texit;\n\t};\n...\n\t# count only successful calls\n\tif (!rl_check(\"gw_$ru\", 100)) {\n\t\trl_dec_count(\"gw_$ru\");\n\t\tsl_send_reply(503, \"Server Unavailable\");\n\t\texit;\n\t};",
          "description": "rl_check usage"
        }
      ],
      "related_functions": [
        "rl_dec_count"
      ],
      "deprecated": false
    },
    {
      "name": "rl_dec_count",
      "signature": "rl_dec_count(name)",
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "identifies the name of the pipe."
        }
      ],
      "return_type": "",
      "description": "This function decreases a counter that could have been previously increased by rl_check function.",
      "usage_context": [
        "REQUEST_ROUTE",
        "FAILURE_ROUTE",
        "ONREPLY_ROUTE",
        "BRANCH_ROUTE",
        "ERROR_ROUTE",
        "LOCAL_ROUTE",
        "TIMER_ROUTE",
        "EVENT_ROUTE"
      ],
      "examples": [
        {
          "language": "opensips",
          "code": "\tif (!rl_check(\"gw_$ru\", 100, \"TAILDROP\")) {\n\t\texit;\n\t} else {\n\t\trl_dec_count(\"gw_$ru\");\n\t};",
          "description": "rl_dec_count usage"
        }
      ],
      "related_functions": [
        "rl_check"
      ],
      "deprecated": false
    },
    {
      "name": "rl_reset_count",
      "signature": "rl_reset_count(name)",
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "identifies the name of the pipe."
        }
      ],
      "return_type": "",
      "description": "This function resets a counter that could have been previously increased by rl_check function.",
      "usage_context": [
        "REQUEST_ROUTE",
        "FAILURE_ROUTE",
        "ONREPLY_ROUTE",
        "BRANCH_ROUTE",
        "ERROR_ROUTE",
        "LOCAL_ROUTE",
        "TIMER_ROUTE",
        "EVENT_ROUTE"
      ],
      "examples": [
        {
          "language": "opensips",
          "code": "\tif (!rl_check(\"gw_$ru\", 100, \"TAILDROP\")) {\n\t\texit;\n\t} else {\n\t\trl_reset_count(\"gw_$ru\");\n\t};",
          "description": "rl_reset_count usage"
        }
      ],
      "related_functions": [
        "rl_check"
      ],
      "deprecated": false
    }
  ],
  "exported_mi_functions": [
    {
      "name": "rl_list",
      "parameters": [
        {
          "name": "pipe",
          "type": "string",
          "required": false,
          "description": "indicates the name of the single pipe to be listed."
        },
        {
          "name": "filter",
          "type": "string",
          "required": false,
          "description": "a pattern used to filter the active pipes to be listed. The filter is a shell wildcard pattern (see glob(7))."
        },
        {
          "name": "filter_out",
          "type": "string",
          "required": false,
          "description": "a pattern used to filter out the active pipes NOT to be listed. The filter is a shell wildcard pattern (see glob(7))."
        }
      ],
      "description": "Lists the parameters and variabiles in the ratelimit module.\n\nNote that you cannot combine multiple paramters when calling this function. If using parameters, only one is accepted.\n\nIf no parameter are passed to the function, all the active pipes are listed.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_list pipe=gw\\_10.0.0.1",
          "description": ""
        },
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_list filter=gw\\_*",
          "description": ""
        }
      ]
    },
    {
      "name": "rl_dump_pipe",
      "parameters": [
        {
          "name": "pipe",
          "type": "string",
          "required": true,
          "description": "indicates the name of the pipe."
        }
      ],
      "description": "Exposes all the details about the current runtime data (specific to the pipe's algorithm) of a pipe. Currently make sense for SBT.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_dump\\_pipe gw\\_10.0.0.1",
          "description": ""
        }
      ]
    },
    {
      "name": "rl_reset_pipe",
      "parameters": [
        {
          "name": "pipe",
          "type": "string",
          "required": true,
          "description": "indicates the name of the pipe whose counter should be reset."
        }
      ],
      "description": "Resets the counter of a specified pipe.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_reset\\_pipe gw\\_10.0.0.1",
          "description": ""
        }
      ]
    },
    {
      "name": "rl_set_pid",
      "parameters": [
        {
          "name": "ki",
          "type": "number",
          "required": true,
          "description": "the integral parameter."
        },
        {
          "name": "kp",
          "type": "number",
          "required": true,
          "description": "the proportional parameter."
        },
        {
          "name": "kd",
          "type": "number",
          "required": true,
          "description": "the derivative parameter."
        }
      ],
      "description": "Sets the PID Controller parameters for the Feedback Algorithm.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_set\\_pid 0.5 0.5 0.5",
          "description": ""
        }
      ]
    },
    {
      "name": "rl_get_pid",
      "parameters": [],
      "description": "Gets the list of in use PID Controller parameters.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_get\\_pid",
          "description": ""
        }
      ]
    },
    {
      "name": "rl_bin_status",
      "parameters": [],
      "description": "Dumps each destination used for replication, as well as the timestamp of the last message received from them.",
      "examples": [
        {
          "language": "opensips-cli",
          "code": "opensips-cli -x mi rl\\_bin\\_status",
          "description": ""
        }
      ]
    }
  ],
  "exported_statistics": [],
  "exported_events": [],
  "exported_pseudo_variables": [
    {
      "name": "$rl_count(name)",
      "type": "integer",
      "readable": true,
      "writable": false,
      "scope": "",
      "description": "Returns the counter of a pipe. The variable is read-only.\n\nNULL will be returned if the pipe does not exist."
    }
  ],
  "configuration_examples": [
    {
      "title": "Set `timer_interval` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"timer\\_interval\", 5)\n..."
    },
    {
      "title": "Set `limit_per_interval` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"limit\\_per\\_interval\", 1)\n..."
    },
    {
      "title": "Set `expire_time` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"expire\\_time\", 1800)\n..."
    },
    {
      "title": "Set `hash_size` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"hash\\_size\", 512)\n..."
    },
    {
      "title": "Set `default_algorithm` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"default\\_algorithm\", \"RED\")\n..."
    },
    {
      "title": "Set `cachedb_url` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"cachedb\\_url\", \"redis://root:root@127.0.0.1/\")\n..."
    },
    {
      "title": "Set `db_prefix` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"db\\_prefix\", \"ratelimit\\_\")\n..."
    },
    {
      "title": "Set `repl_buffer_threshold` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"repl\\_buffer\\_threshold\", 500)\n..."
    },
    {
      "title": "Set `repl_timer_interval` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"repl\\_timer\\_interval\", 100)\n..."
    },
    {
      "title": "Set `repl_timer_expire` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"repl\\_timer\\_expire\", 10)\n..."
    },
    {
      "title": "Set `pipe_replication_cluster` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"pipe\\_replication\\_cluster\", 1)\n..."
    },
    {
      "title": "Set `window_size` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"window\\_size\", 5)\n..."
    },
    {
      "title": "Set `slot_period` parameter",
      "description": "",
      "code": "...\nmodparam(\"ratelimit\", \"window\\_size\", 5)\n#we will have 50 slots of 100 milliseconds\nmodparam(\"ratelimit\", \"slot\\_period\", 100)\n..."
    },
    {
      "title": "`rl_check` usage",
      "description": "",
      "code": "...\n\t# perform a pipe match for all INVITE methods using RED algorithm\n\tif (is\\_method(\"INVITE\")) {\n\t\tif (!rl\\_check(\"pipe\\_INVITE\", 100, \"RED\")) {\n\t\t\tsl\\_send\\_reply(503, \"Server Unavailable\");\n\t\t\texit;\n\t\t};\n\t};\n...\n\t# use default algorithm for each different gateway\n\t$var(limit) = 10;\n\tif (!rl\\_check(\"gw\\_$ru\", $var(limit))) {\n\t\tsl\\_send\\_reply(503, \"Server Unavailable\");\n\t\texit;\n\t};\n...\n\t# count only successful calls\n\tif (!rl\\_check(\"gw\\_$ru\", 100)) {\n\t\trl\\_dec\\_count(\"gw\\_$ru\");\n\t\tsl\\_send\\_reply(503, \"Server Unavailable\");\n\t\texit;\n\t};\n..."
    },
    {
      "title": "`rl_dec_count` usage",
      "description": "",
      "code": "...\n\tif (!rl\\_check(\"gw\\_$ru\", 100, \"TAILDROP\")) {\n\t\texit;\n\t} else {\n\t\trl\\_dec\\_count(\"gw\\_$ru\");\n\t};\n..."
    },
    {
      "title": "`rl_reset_count` usage",
      "description": "",
      "code": "...\n\tif (!rl\\_check(\"gw\\_$ru\", 100, \"TAILDROP\")) {\n\t\texit;\n\t} else {\n\t\trl\\_reset\\_count(\"gw\\_$ru\");\n\t};\n..."
    }
  ]
}