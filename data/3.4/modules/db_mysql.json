{
  "id": "module-db_mysql-3.4",
  "document_type": "module",
  "version": "3.4",
  "title": "db_mysql Module",
  "url": "https://opensips.org/html/docs/modules/3.4.x/db_mysql.html",
  "content_markdown": "This is a module which provides MySQL connectivity for OpenSIPS. It implements the DB API defined in OpenSIPS.",
  "category": "Modules",
  "extracted_at": "2026-04-25T09:46:19.093Z",
  "module_name": "db_mysql",
  "overview": "This is a module which provides MySQL connectivity for OpenSIPS. It implements the DB API defined in OpenSIPS.",
  "how_it_works": null,
  "dependencies_required": [
    {
      "name": "tls_mgm",
      "type": "module",
      "reason": "Required if use_tls is defined",
      "optional": true
    },
    {
      "name": "libmysqlclient-dev",
      "type": "library",
      "reason": "The development libraries of mysql-client",
      "optional": false
    }
  ],
  "dependencies_optional": null,
  "exported_parameters": [
    {
      "name": "exec_query_threshold",
      "type": "integer",
      "default_value": "0 - disabled",
      "description": "If queries take longer than 'exec_query_threshold' microseconds, warning messages will be written to logging facility.",
      "example_value": "60000",
      "example_code": "modparam(\"db_mysql\", \"exec_query_threshold\", 60000)"
    },
    {
      "name": "timeout_interval",
      "type": "integer",
      "default_value": "2 (6 sec)",
      "description": "Time interval after which a connection attempt (read or write request) is aborted. The value counts three times, as several retries are done from the driver before it gives up.\n\nThe read timeout parameter is ignored on driver versions prior to “5.1.12”, “5.0.25” and “4.1.22”. The write timeout parameter is ignored on version prior to “5.1.12” and “5.0.25”, the “4.1” release don't support it at all.",
      "example_value": "2",
      "example_code": "modparam(\"db_mysql\", \"timeout_interval\", 2)"
    },
    {
      "name": "max_db_queries",
      "type": "integer",
      "default_value": "2",
      "description": "The maximum number of retries to execute a failed query due to connections problems. If this parameter is set improperly, it is set to default value.",
      "example_value": "2",
      "example_code": "modparam(\"db_mysql\", \"max_db_queries\", 2)"
    },
    {
      "name": "max_db_retries",
      "type": "integer",
      "default_value": "3",
      "description": "The maximum number of database connection retries. If this parameter is set improperly, it is set to default value.",
      "example_value": "2",
      "example_code": "modparam(\"db_mysql\", \"max_db_retries\", 2)"
    },
    {
      "name": "ps_max_col_size",
      "type": "integer",
      "default_value": "1024 (bytes)",
      "description": "The maximum size of a column's data, when fetched using prepared statements. Particularly relevant for variable-length data, such as CHAR, BLOB, etc.",
      "example_value": "4096",
      "example_code": "modparam(\"db_mysql\", \"ps_max_col_size\", 4096)",
      "notes": "NOTE: Should a column's data exceed this limit, the value will be silently truncated to fit the buffer, without reporting any errors!"
    },
    {
      "name": "use_tls",
      "type": "integer",
      "default_value": "0 (not enabled)",
      "description": "Setting this parameter will allow you to use TLS for MySQL connections. In order to enable TLS for a specific connection, you can use the \"**tls_domain=**dom_name\" URL parameter in the db_url of the respective OpenSIPS module. This should be placed at the end of the URL after the **'?'** character. Additionally, the query string may include the \"**tls_opts=** PKEY,CERT,CA,CA_DIR,CIPHERS\" CSV parameter, in order to control/limit the amount of TLS options passed to the TLS library.\n\nWhen using this parameter, you must also ensure that _tls_mgm_ is loaded and properly configured. Refer to the the module for additional info regarding TLS client domains.\n\nNote that if you want to use this feature, the TLS domain must be provisioned in the configuration file, _NOT_ in the database. In case you are loading TLS certificates from the database, you must at least define one domain in the configuration script, to use for the initial connection to the DB.\n\nAlso, you can _NOT_ enable TLS for the connection to the database of the _tls_mgm_ module itself.",
      "example_value": "1",
      "example_code": "modparam(\"tls_mgm\", \"client_domain\", \"dom1\")\nmodparam(\"tls_mgm\", \"certificate\", \"[dom1]/etc/pki/tls/certs/opensips.pem\")\nmodparam(\"tls_mgm\", \"private_key\", \"[dom1]/etc/pki/tls/private/opensips.key\")\nmodparam(\"tls_mgm\", \"ca_list\",     \"[dom1]/etc/pki/tls/certs/ca.pem\")\n...\nmodparam(\"db_mysql\", \"use_tls\", 1)\n...\nmodparam(\"usrloc\", \"db_url\", \"mysql://root:1234@localhost/opensips?tls_domain=dom1\")\n...\nmodparam(\"usrloc\", \"db_url\", \"mysql://root:1234@localhost/opensips?tls_domain=dom1&tls_opts=PKEY,CERT,CA,CA_DIR,CIPHERS\")"
    }
  ],
  "exported_functions": [],
  "exported_mi_functions": [],
  "exported_statistics": [],
  "exported_events": [
    {
      "name": "E_MYSQL_CONNECTION",
      "description": "This event is raised when a MySQL connection is lost or recovered.",
      "parameters": [
        {
          "name": "url",
          "type": "string",
          "description": "the URL of the connection as specified by the _db_url_ parameter."
        },
        {
          "name": "status",
          "type": "string",
          "description": "_connected_ if the connection recovered, or _disconnected_ if the connection was lost."
        }
      ]
    }
  ],
  "exported_pseudo_variables": [],
  "configuration_examples": [
    {
      "title": "Set `exec_query_threshold` parameter",
      "description": "If queries take longer than 'exec_query_threshold' microseconds, warning messages will be written to logging facility.",
      "code": "...\nmodparam(\"db_mysql\", \"exec_query_threshold\", 60000)\n..."
    },
    {
      "title": "Set `timeout_interval` parameter",
      "description": "Time interval after which a connection attempt (read or write request) is aborted. The value counts three times, as several retries are done from the driver before it gives up.",
      "code": "...\nmodparam(\"db_mysql\", \"timeout_interval\", 2)\n..."
    },
    {
      "title": "Set `max_db_queries` parameter",
      "description": "The maximum number of retries to execute a failed query due to connections problems. If this parameter is set improperly, it is set to default value.",
      "code": "...\nmodparam(\"db_mysql\", \"max_db_queries\", 2)\n..."
    },
    {
      "title": "Set `max_db_retries` parameter",
      "description": "The maximum number of database connection retries. If this parameter is set improperly, it is set to default value.",
      "code": "...\nmodparam(\"db_mysql\", \"max_db_retries\", 2)\n..."
    },
    {
      "title": "Set `ps_max_col_size` parameter",
      "description": "The maximum size of a column's data, when fetched using prepared statements. Particularly relevant for variable-length data, such as CHAR, BLOB, etc.",
      "code": "...\nmodparam(\"db_mysql\", \"ps_max_col_size\", 4096)\n..."
    },
    {
      "title": "Set the `use_tls` parameter",
      "description": "Setting this parameter will allow you to use TLS for MySQL connections. In order to enable TLS for a specific connection, you can use the \"tls_domain=dom_name\" URL parameter in the db_url of the respective OpenSIPS module. This should be placed at the end of the URL after the '?' character. Additionally, the query string may include the \"tls_opts= PKEY,CERT,CA,CA_DIR,CIPHERS\" CSV parameter, in order to control/limit the amount of TLS options passed to the TLS library.",
      "code": "...\nmodparam(\"tls_mgm\", \"client_domain\", \"dom1\")\nmodparam(\"tls_mgm\", \"certificate\", \"\\[dom1\\]/etc/pki/tls/certs/opensips.pem\")\nmodparam(\"tls_mgm\", \"private_key\", \"\\[dom1\\]/etc/pki/tls/private/opensips.key\")\nmodparam(\"tls_mgm\", \"ca_list\",     \"\\[dom1\\]/etc/pki/tls/certs/ca.pem\")\n...\nmodparam(\"db_mysql\", \"use_tls\", 1)\n...\nmodparam(\"usrloc\", \"db_url\", \"mysql://root:1234@localhost/opensips?tls_domain=dom1\")\n...\nmodparam(\"usrloc\", \"db_url\", \"mysql://root:1234@localhost/opensips?tls_domain=dom1&tls_opts=PKEY,CERT,CA,CA_DIR,CIPHERS\")\n..."
    }
  ]
}